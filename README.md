# Homework4

”становите переменные окружени€ PGUSER, PGPASSWORD (и PGPORT, если необходимо), т.к. в ConnectionString логин/пароль не указаны.  
¬ыполните Create.sql. ≈сли будете выполн€ть его из командной строки и возникнут проблемы с кодировкой русских символов в базе, установите перед запуском скрипта переменную окружени€ PGCLIENTENCODING = UTF8.  
ѕредупреждение: Create.sql удал€ет и создаЄт базу SVI_Db4, т.е. данные, если были в базе, будут утер€ны.  
